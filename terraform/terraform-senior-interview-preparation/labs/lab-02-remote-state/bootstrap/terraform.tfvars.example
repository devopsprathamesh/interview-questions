# Copy to terraform.tfvars. state_bucket_name MUST be globally unique across all of AWS.

aws_region        = "us-east-1"
state_bucket_name = "my-org-tf-state-CHANGE-ME-1234"
lock_table_name   = "terraform-state-lock"
