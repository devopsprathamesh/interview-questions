---
plugin: amazon.aws.aws_ec2
regions:
  - us-east-1
filters:
  instance-state-name: running
  tag:Project: ansible-lab07
keyed_groups:
  - key: tags.Environment
    prefix: tag_Environment
compose:
  ansible_host: private_ip_address
