prometheus:
  prometheusSpec:
    retention: 15d
    resources:
      requests: { cpu: 250m, memory: 512Mi }
      limits: { cpu: 1, memory: 1Gi }

grafana:
  adminPassword: "changeme-lab-only"
  resources:
    requests: { cpu: 100m, memory: 256Mi }
