apiVersion: v1
kind: Service
metadata:
  name: bad-internet-facing-service
  namespace: internal-only
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-scheme: internet-facing   # VIOLATION
spec:
  type: LoadBalancer
  selector: { app: demo }
  ports: [{ port: 80 }]
