apiVersion: v1
kind: Service
metadata:
  name: good-internal-service
  namespace: internal-only
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-scheme: internal
spec:
  type: LoadBalancer
  selector: { app: demo }
  ports: [{ port: 80 }]
