# Illustrative "broken" image reference for triggering the canary test -
# in practice, build a tiny app image that returns 500 for all requests,
# tag it broken-image:v2, and push to a registry your cluster can pull from.
#
# Example minimal broken app (build and push before running this lab):
#   FROM python:3.11-slim
#   RUN pip install flask
#   COPY app.py .
#   CMD ["python", "app.py"]
#
# app.py:
#   from flask import Flask
#   app = Flask(__name__)
#   @app.route("/")
#   def broken():
#       return "Internal Server Error", 500
#   app.run(host="0.0.0.0", port=80)
